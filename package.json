{
  "name": "simple-copy-link",
  "version": "1.0.3",
  "description": "very simple copy link web extension",
  "scripts": {
    "build": "parcel build --no-optimize manifest.json",
    "clean": "rm -rf dist/ .parcel-cache",
    "release": "yarn clean && parcel build manifest.json",
    "report": "parcel build manifest.json --reporter @parcel/reporter-bundle-analyzer",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "https://github.com/tkhm/simple-copy-link.git",
  "author": "tkhm",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@parcel/config-webextension": "^2.13.0",
    "@parcel/reporter-bundle-analyzer": "^2.13.0",
    "@types/chrome": "^0.0.293",
    "parcel": "^2.13.0"
  },
  "dependencies": {}
}
